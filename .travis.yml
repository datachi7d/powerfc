language: c
addons:
 apt:
  sources:
   - george-edison55-precise-backports
   - ubuntu-toolchain-r-test
  packages:
   - cmake
   - cmake-data
   - gcc-5
   - g++-5
   - python-virtualenv
   - lcov
   - socat
   - gengetopt
   - valgrind
compiler:
  - gcc
before_install:
  - pip install --user cpp-coveralls
script:
  - make GCOV=1 gcov
after_success:
  - coveralls --exclude lib --exclude build --exclude src/tests --exclude src/tools --gcov-options '\-lp' --verbose
